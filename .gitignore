dist
node_modules
package-lock.json

# Vendored at build from @fontsource (see sync-fonts.js)
assets/static/fonts/*.woff2

# Untrusted candidate data — input to verification, never shipped (see scripts/)
scripts/countries.candidate.json

.DS_Store
**/.DS_Store

# Output of 'npm pack'
*.tgz
